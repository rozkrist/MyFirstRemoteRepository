#Write a program to sort the list (without using sort function). Any algorithm can be implemented

list = [2, 3, -1, 1, 0, -9, -4]

for i in range(len(list)):
    for j in range(i + 1, len(list)):

        if list[i] > list[j]:
            list[i], list[j] = list[j], list[i]

print(list)